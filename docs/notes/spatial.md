I am dissatisfied with our current setup.Right now we have a logs window, a jobs window, and a timeline window. And the logs window has the log messages kind of going off the screen. And the jobs window is kind of only hard coded for what the timeline can support with the track for transcription.We also added toasts that are based off of logging, which is awesome, and I think that's a better situation than the jobs window.However, one of the things that we want to track is progress. So if we had a progress hub window where we would be able to analyze the logs and extract messages that indicate progress on tasks or like when a job is started, we could create a job system through structured logging rather than actually hard coding the job structure as we have done.So in the sake of not maintaining things that we don't want anymore, we can rip out the jobs system that we added and instead ensure that we convert all of those status signals to log messages.and then we can have our progress hub be able to detectthose log messages andcreate.a window for each progress item. So that's another thing is that right now we have one timeline window, but really I think we want to be more granular and more window based in our approach.So for example, in our timeline window, we have the track list, the control buttons, and then the actual timeline where there's the play button and then there's the ruler area.CoughingSo each track could be its own window.where that window isof a long horizontal not very tall thing and we can reposition it that way where on that window we would havetrack name and those controls and stuff in the icon but we would also be able to havetheelements where inside the track there are clips. So I think what we want to promote is a system that is distributed across windows rather than large windows doing everything and owning all the behavior. Because by decomposing things into smaller windows that communicate with each other, we are able to make changes and focus on smaller areas at once.Part of the way that this works or will work well is that we need highly sophisticated spatial reasoning for the system.And right now we have the cursor thing.The cursor info window gives us some diagnostic ability to inspect the cursor and the size of it in the active area, but what we don't have is asomething that is a cohesive system that's aware of the purposes of our individual windows and stuff. So if we have a button inside a window,Then the area that button occupies is something we would want to be able to inspect through reflection. And to do reflection we have access to the facet crate. So if we store the sizes and locations of our buttons in a struct,andthen we can explore those fields and interpret them spatially whereif we are able toReinterpret.the struct asnumbers, then...We can interpret any three tuple of numbers as an XYZ vector or an RGB value. So thinking about this timeline view as a way tocoordinate messages so that we can inspect them.If we were to decompose this timeline into the different parts, then right now we have a track list and the buttons and then the kind of the body of the timeline.So each of those could become a window.And then you run into the question of what happens when the list of tracks and the actual tracks in the timeline aren't aligned spatially.The answer might just be that we don't care.And also if a frack listis a list of items and each itemcould in theory be its own window, then...What is the implication of that?My understanding is that right now we have kind of a scene system where there's kind of a class attached to the windows that we have open and that describes how things are being rendered.When.software is compositions of islands of behavior.So we might have the chrome island where at the top of our window we have the pin window button, the title, the grab area for us to reposition it, the minimize, restore, maximize, and the close buttons. And then we've also introduced this kind of diagnostics button where itreinterprets the window using a terminal user interface.And we actually have a working terminal where we own kind of everything about it and we usedof that logic to create the cursor info interface where it uses ratatouille without actually going through a sub-process so that we can minimize the layers of indirection because in that cursor info window the part we care about is kind of rendering this touille instead of beingall in onhaving it be a more graphical application. And I don't think we've nailed the right hybrid approach, where being able to switch back and forth between terminal representations and nonIt doesn't feel quite right. I think the ratatouille abstraction for spatially partitioning things and using these kind of terminal grids is appropriate.We also havesituations like the logs window where the log messages are kind of going out of the screen. So the definition on what the viewports or the cell grids are in terms ofDo they have a height and a width? Or is that something that is kind of more arbitrarily defined, which means we have more of a one-dimensional stream of virtual terminal instructions, which could be something like resizing the grid or emitting special codes that transmit messages like we've returned to the prompt kind of thing.So I thinkThe important part is the spatial partitioning, where in Ratatouille it's grid-based, and in our UI it is alsogrid. It's grid aligned or axis aligned wherein this coordinate system we haveThe space kind of partitioned and we have buttons that are circles, but really they are positioned in a square way so What we should be focusing on is maximum integration withthe cell grid kind of rendering style with Ratatouille.And then as part of that, we want to enforce a behavior that lets us draw higher fidelity informationSo if we have one grid square or two grid squares where we are saying like, this is the settings button, then that is square, whereas in the terminal, the grid squares are kind oftaller than they are wide.which puts you in weird situations where you can, using Unicode and the foreground and background color, draw sub-cell pixels and fit two pixels into one terminal grid cell. But that means you can't also be writing text in that cell, which makes it a little awkward.So, what?interesting isdescribing the UI in terms ofa hierarchy.whereHierarchy is a partitioning. So if we say this window has a left side and a right side then on the right sidewe could say there is a top side and a bottom side. And then in the top side we say we're going to render a graphic here.So it's similar to...Ratatouille in terms of slicing up the space and then it's similar toI don't know, some just immediate mode rendering paradigm. I don't know if Igui does it exactly this way or what, but really you have thearea to work in and then you can kind of do stuff.whichThe question, how do things like tooltips that render outside the area work?Taking our toasts as an example, we have this window that positions itself outside the main window and is spatially aware of the different windows from where toasts could come from.So if we think about what a playground would look like that demonstrates the fullness of this behavior, if we were to add a main menu button called toast playground, then clicking that button would open three different windows, and each window has a button that says create toast, and that would launch a toast that is positioned adjacent to that window. So those three windows that we open, we would spatially distribute them so that they don't overlap.Since we own the fullness of the rendering stack, we can play with our windows and interchange the material and say,This window is the timeline window and it's rendering a certain content, but at a moment's notice we could say it's doing something entirely different.So having.The operating system windows is part of the design and then having algorithms for spatial reasoning is another huge part becausethings that we want to express, like tooltips.The surface area of a tooltip should not intersect the surface area of the cursor so that the content in the tooltip is always renderable.And then there's a meta layer where we might want to select the text in the tooltip.So then perhaps we want a way to freeze the tooltips so that it cannot run away from the cursor as we approach it to select the content inside.And then that reiterates that we have a kind ofbehavioral expectation that we can select text when it is visible and with doing Islands of Behavior where we have this kind of terminal abstraction that lets us select text, we want to be able toselect the text in the tooltip and copy it so those expectations we want to hold so the degree to which we want tosay that everything is just a terminal grid, I think the more we adopt that.more cohesive the design will be, but we also don't want to lose the ability to sayThis spot is where we're going to render something with shaders that is going to be more complex.Geometry.So right now when we have the Diagnostics view of a page, it's kind of a separate Ratatouille app when really we want tounify the experience so that every app is that way andideally we can turn it off orhave it be kind of a dumb representation where if we wanted to copy the state of the system as plain text then the icon for a gear that indicates this is a settings button could be replaced withto cells that say settings or something liketheability to attach annotations to stuff so like tooltipsbutton kind of knows what its tooltip is going to be, even if it's not showing it. And if I wanted to say, screenshot this window, then there's a lot of pixels in that window that are notconveying information, whereas the text in the tooltip is kind of high degree of information.if we haveVirtual terminal sequence or whatever that becomes the window snapshot thenThat output behavior issomething whereIt might render to a terminal, but it could be usingcodes that emit non-renderable things to encode that there is a tooltip on this location of the screen.So I think part of thesituation we have found ourselves in is that we have iterated and designed some features that pretty muchkind of work but the mutation of those features to add functionality or the future direction of the app is kind of constrained to the current design and having a menu button for playgrounds where we could have the toast playground and a spatial reasoning playground issomething that is worthwhile pursuing and we'd also have aterminal playground where we couldUse or demonstrate the functionality of the terminal stuffto play back key sequences or like to observe all the events of the system and say like this is a button with a tooltip just as a demonstration and as a gallery forthethat we are reusing across our application.So if we have a button or a shader effect for rendering a button, then we should have a button playground that isa gallery of all the differentbuttons that our app would have in terms of just the rendering and the hover behavior. So being able to describe that every part of our application we want to beresponsive and dynamic so that when the user hovers over a button, we never accidentally develop a button that doesn't change the cursor icon when you hover over it, orbuttons that have like special shader effects when the the cursor gets near we want to demonstrate those in a way that lets us know they exist and reuse them by having them in a central spot so that when we try and do a button again the second time i don't have to tell you that we already did a button that does xyz because if you look at the gallery you will have an observation of every button in our app already. with the idea of decomposing things into their own windows that kind of relates to the island's idea where if we have panels in an interface or a tiled interface you should be able to rip a tile out of that interface and have it become its own window and conversely you should be able to drag a tile into a window and have it merge back into it. One of the options that we have for doing this is the Windows key. If we Windows key and click and drag something, when we release the key, the default start menu behavior activates, but presumably we can hook and suppress that default behavior so that we can use the Windows key as a language convention of our, the language of our application.that we talk with the user and say that if you want to rip out a panel, then you can Windows key and click and drag anything and that always corresponds to repositioning an element in terms of windowing and paneling.We could say Windows key plus scroll wheel would increase the size of a window in the dimension that your mouse is closest to. So if my mouse is closest to the top of the window, it would scroll up, would increase the window along that dimension.And if we're at the bottom of the window, then scrolling down would make the window longer in that dimension. And if we're at the right of the window, then scrolling up would expand it to the right where theSpatial reasoning of it is kind of up means increase backScroll means decrease.But if we're at the bottom of the window, then maybe we'd want to invert it just for that case, just because scrolling down to go down is kind of neat. Something else that comes to mind is with the timeline and the positioning of windows as controlled by the operating system, there's a lot of flack that we get where trying to drag a window and let it be repositioned. Like if we wanted to programmatically control 27 different windows because we have four tracks and there's a bunch of clips on those tracks being arranged in a timeline way, then repositioning those windows  has calls to the operating system is not going to be as smooth as if we owned one window that encompassed every monitor and we did our own spatial reasoning from there.I think what we'd want to do is to write a skill that describes expectations for UIthat is presented to the user whereIn those expectations we would describe how all text must be able to be selected with click and drag and then ctrl C and when you select text there is a highlight behavior that delineates what is selected from what is not.and when you hover overlike anything that interacts with the mouse pointer hasBehaviors for when the pointer is approaching it when it's hovering it when it's clicking itand...those things would be encoded in this skill so that we could remind ourselves and say like when we're doing something we want to have animations and transitions over time. one thing that came up which is the using the right click to pan the timeline while at the same time we have the scroll wheel to adjust the zoom level and thebehavior interaction of that where while we're holding right click and zooming isthe animation system might have been chugging with that a little bit so havingthe effect and the animation being...detached almost so that we can override it when we don't want it to happen, but that might be a bit more specific than we need to go into for common behaviors in our app.


We were were doing windowing ealier using thread_locals and stuff where we had Arc<Mutex<T>> in some locations and it was causing correctness issues with deadlocking that were hard to diagnose. Encoding real state machines, push-down automatas, message passing to describe the way the system operates is also helpful.

We can imagine that if we have a island and we want that island to be repositionable then these islands are described using floating-origin techniques so we could re-implement title bars and click-to-drag behaviour on our own.

---

I think an intermediary playground is in order since the progress hub will reuse many parts of UX design that we already have in disparate areas without centralized discovery mechanisms. The jobs window will need to support scrolling, and our terminal already has scrolling. Scrolling behaviours imply scroll bars. Another way to think about it is that the progress hub is simply another timeline view, where we can introduce a job timeline where we reuse parts of the timeline we have in terms of nanosecond precision, panning with right mouse, zooming in and out with cursor, and we introduce the jobs into that where when a job starts we know the start point and eventually the end point will cap it off but otherwise when the job starts the visual of it will grow according to the elapsed time, so there's the job description and the job duration, we can have the description appear to the left of the clip that represents the job duration. Actually, the job description is data that could live in its own window AND in a popup so when we look at the job view we have a timeline that shows the jobs where for now each job gets a new row and we can have a button in the top right like "follow latest job" that will automatically pan the camera and the clips for a job can have text on them for the quick name of the job and when we hover them it will show the details of the job and clicking the job will solidify the tooltip to something that gets a title bar and stays when we stop hovering the job thing; it gets its own window.
Our current timeline is "starting from 0" like a new document where the job timeline is more like a log timeline where we are observing our own logs and we can have a toggle for "jobs only" and a toggle button for "follow latest" where if the jobs filter is off we can follow all the events from tracing otherwise we can filter the display and the following to just job-events where the rows are determined by span names and we can have little carets for the log events since log events have no duration unlike log spans and job spans that are inferred; I guess jobs are always spans so it's a property of a span whether its a job or not but even that might not be a necessary distinction if we say "by default, we only show the user info and above level spans and events" that way we aren't cluttering the user with more but for reasons we can have a way to filter them

