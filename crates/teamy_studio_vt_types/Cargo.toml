[package]
name = "teamy_studio_vt_types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
libghostty-vt = { workspace = true, optional = true }

[features]
default = []
ghostty = ["dep:libghostty-vt"]
