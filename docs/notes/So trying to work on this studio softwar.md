 So trying to work on this studio software where it's going to be a
whole project self-encompassed that lets you do many things and some of the pieces that we're missing include a

like a text editor, a file browser, a voice transcription tool, and the decomposition of big problems into small problems includes the language and grammars that we create for human-computer interaction.and thecommon element of all of this is that when the human is interacting, the computer has a list of valid actions that they're soliciting the human pick from. So the pick action where multiple choices are presented, and the user can pickzero or more of those choices.and the outcome of that isperhaps picking zero choices is interpreted as a cancellation, or picking more than one choice is interpreted as a invalid answer if it's only looking for one choice. But the interface that we must design adheres to the common contract of picking zero or more choices.And... in the cloud_terrastodon_user_input crate I've made a PickerTui that covers this pretty well

So if we imagine the different kind of inputs, then in HTML land, you have one input element and then you can change it to be like a numeric input, or you could have a select element to pick from a pre-selected list of options, or you could have a text box or a text area for free form input.  And very often there are additional validations imposed upon those inputs where if there's a postal code or a credit card number, there's a function that determines if it's valid or not. And as you are stochastically typing, the characters that you're picking from constrain what the future characters would produce a valid final result. So if we imagine thatit's all a stochastic input system, then for some decision points in the system,you could try and model it as a key down situation where it's likeIf only ABC are valid characters, then instead of just a freeform text box where you perform a post hoc validation and say you entered the wrong characters, you would only allow them to enter the correct character. So if you have three choices to pick from, you couldprovide them choices one, two, three so they could only hit one, two or three on the keyboard.But that isbringing the keyboard and mapping it directly to an outcome when there exists the middle abstraction on the actual action. So if we imagine how Visual Studio Code works is there's a bunch of actions which can be displayed easily by using the Ctrl Shift P command palette.and the mapping between keyboard or mouse and then to the action issomething that we want to be user customizable.And in the different systems like Visual Studio Code and Minecraft where there exists so many keyboard actions that there are conflicts even out of the box sometimes, then we can imagine that a better starting state would be if nothing is bound and you have to add the bindings. And that isnot entirely possible because you still have to introduce some kind of mechanism. So if we're doing keyboard navigation as a primary focus, then we would want to have the arrow keys and maybe blasdy or hjkl bound to be up down left right in terms of manipulating a focus.So in many UI stuff there is the idea of a focused window or a focused element for the purposes of tab navigation.And if we imagine a multiplayer user interface, then if it's like aplaying with your sibling on one computer you might have two different focuses where I'm red and you're blue and we can each select different things so computers don't reallyare designed for multiple cursors, they're not really designed for that right now, but given agent-human interaction, we want to design a system that does allow for multiple inputs andif wethe interfaces that we have, then right now this Timmy Studio applicationNote that I'm using the voice-to-text, so T-E-A-M-Y is frequently misinterpreted.and the input space.is on the main menu, there's some amount of buttons with little icons that go with them. They're not even little, but just like graphical representations along with their labels. And you can click on them. And that constitutes a selection and a invocation, like theconceptualization on the user interface. If I have a terminal button, a cursor info button, a storage button, and an audio button, then the outcomes for clicking those buttons are launch terminal, launch cursor info, launch storage, and launch audio.and on a different layer of abstraction is what buttons areselected, and then a general invoke action, and the invoke action gets translated into those other actions. And perhaps we'd want a keyboard shortcut that would let us directly launch the terminalAnd in some situations, we might have multiple windows open and the operating system has the idea of one of these windows being focused, but because all of the windows are controlled by us, we own how the keyboard inputs are being interpreted.One interesting thing was the creation ofSome word processor one of the motivating mantras of the creator was no modes so in VIN you have insert mode orvisual mode or like whatever and having things not being locked into a mode isaway to design.interfaces in one style, butif you imagine something like a terminal wherewhen we're in the terminal application. If we're using some 2e inside the terminal, and the 2e has a behavior for control D, thenwe don't want our harness to eat that input before it gets to the terminal. So there's also the concept of like a full screen or a greedy application where it would want to receive everything.and the mapping betweenall of these different concepts isif we have.a list of actions or a list of choices. So the choices that we present to a user, in this case, thethings that the app is doing launching a terminal and then that terminal we have works and then launching this cursor info thing which is like a 2e but it's kind of rich so that we can run it without all the terminal backbone if we actually launch it from our app because we have the whole graphics stack to play with where we can pass the inputs and interpret them and produce this kind of cellular output without using the terminal but using theprotocols so we're kind of skipping aAnd...something like a storage browser, we don't even have implemented yet, but we kind of have the button on the main menu for and because we implemented the bell, the control G or whatever in the terminal app, we also have this audio app on the main menu where we can just kind of play a little sound or browse to a sound to have that be played. So the interlinkedness of these things ispartially implemented and notsuper coherently designed, but we also have some interesting things like there's the details view on the different apps. So when we hover over in our Chrome area next to the minimize button, we have the show diagnostics button. And that will provide additional information about the scene. And right now for the terminal, it kind of pops up this box below it. And on the main menu, it switches the the presented content to be instead aUh...So theEntropy of this the scene that's being presented and presenting it in a very condensed text form or just a different format entirely isthe compressed representation on it like really the main menu is asking me between the actions of launch terminal launch cursor info launch storage and launch audio which of those actions do I want to invokeSo...thethe mostfundamental thing is like we have this scene that we are presenting and the scene contains the input space on these actions and the user can pick from them andcreating this app in a way thatgives us reusable building blocks, so if I want topick a microphone, like if I want to rewrite the thing that I'm using to transcribe my voice, thenWe need to...display to the user the list of microphones and have them pick what the microphone they want to to activate for transcription and then that would launch a window where there would be the microphone and a little record button so we can imagine that the microphone is kind of alwayson like it's always plugged in.and the app is...recording that input or not like we can we can enumerate the microphones without actually hooking into the listening API because once we start listening that might turn on an LED or might show something in the status bar sowhile this window is open, we could beimmediately start the listening thing.And then there's also in recording programs, you kind of have the arm, arm for recording, whereYou might want to pause recording for a certain time.So.That's the mechanism that we use to stop and start listening isWhen we click so on the main menu we would add aaudio recording devices button and when you click that that would open a window and that window is asking us to pick from the known audio recording devices which ones we want to proceed withAnd...These actions are...parameterize so that when we have athe next stage, so if I have two microphones, if I have like my desktop and my headset microphone, then it's not like we have a launch desktop microphone window and a launch headset microphone window, it's launch microphone window and then parameterized by which microphone we're listening to.That isone thing and then the other thing is thatThe transcription stuff is like when we're listening to this microphone, there is recording the audio into an in memory buffer and then displaying some kind of waveform thing.And then maybe we want that to be kind of scrubable. And then we want to be able to pass a waveform into something that accepts a waveform for the transcription part. And maybe there's a re sample that happens in between that. So thechoices that are going on there areHow do we decompose this because some of the complex user interfaces, you can imagine that if you try and do everything in one window, then you re implement a window manager, which the operating system already does for us andthe the pick part of the app is like what is the action space of the current window andCan we?convert that between intermediate representations likeIf we have Ratatouille and we're able to create these apps like the cursor info app, then we could implement our main menu as a Ratatouille app.and then thepresentation of the choices and stuff. I know there's the kitty graphics protocol, but we can also dobasically whatever we want because we control the terminal side and the VTT parser or whatever. Like we can do whatever we want in terms of actually implementing this.So if we want torender aimage, thenIf that's going to be like a square image or if we want to designate an area for some other process thenThe.The part to focus on is...What are the next steps?improving.the audio transcription.The urgency on that is...not super urgent because i have the existing one that's working but the existing one is kind of jank because it immediately typewrites whatever it transcribes and it's written in python so like if the mic gets unplugged and replugged there's like a a failure case there where it kind of stops working and i'd rather it be rewritten in rust and rewriting the entiremachine learning model inference stuff for the Whisper X model is something I've briefly explored but haven't completely gotten working end to end. So inside the burnt apple repo is some exploration on that.And then...the...text editor. So we need a text editor. And one of the things that I have observed is that when editing something like Rust, their NVS code exists the Rust Analyzer tool, but it's very slow on large projects. And we could probably get away with something later where the things that we want in a text editor are syntax highlighting and jump to definition. And we could implement jump to definition usingDifferent strategies where we might naively parse some of the language or even just ask a language model and say like, hey, the user clicked on this token. Given what you know about the current situation. Where do we want to jump to andIf it takes one second or whatever for it all to chug through it then I guess that's pretty fine as long as we have it working whereYou're able to launch the app and have everything ready in a couple hundred milliseconds instead of Rust Analyzer taking minutes to resolve something if you have ten different projects open.And...I've also explored some infinite canvas stuff where that was interesting.And...was inside Minecraft as well.So if we're thinking aboutrewriting a text editor to be inside an infant canvas while also having this kind of multi selection multiplayer thing going on.to play with the language model.What's...What's the right way to focus our effort and build stuff ina way thatdoes something kind of novel because we've seen historical software stuff we've been doing it forever and now with all these language models and stuff the big unlocks in terms of super tight AI integrationLike in a chat app, you might have a conversation thread and the title of that thread is generated from some language model that summarizes whatever the first message was and in a desktop application.We want to be able to do small things like that everywhere.So...trying to build out the fundamentals, but also...build the fundamentals in a way that like if we're asking the user for a choice then the choice and the options that are being presented areencapsulated in a way that we could present a language model with that choice instead. So if we're asking the user to do something, then maybe there's a button or some global state that says the AI is going to pick instead of the user here.So where would that state live? Because we can we can totally just present a list of choices to an AI and say pick one, but that should be conditioned on what is the user's current objective or what is the AI's current objective.and where that objective lives isinside a text document somewhere because that's all these conversations are is just big JSON structured text that's beingreformatted into the model specific format becauseThere's like all these conversation APIs and each language model might use different tokens for how the conversation is actually structured. So there's some abstraction there.So if we're going to present a choice and thenask for a thing to be picked from those choices, then the context on whattheadditional info is while making that choice isaSo if we have a text editor, we can imagine that we have a window that is displaying text. And maybe that text is read only, or at least this windows view of that text is read only. So then we have one window that's asking for a choice and another window that is context for that choice.So if we imagine an agentic scenario where we launch the studio app and thenthe context is like the studio app was launched at whatever timestamp then maybemy goal when I launched that app was something that I narrated to a text file or something. So like theIf we imagine a concrete outcome, because if we're just building a system, we can build it in any direction we want. But if I have a goal like replace VS code for my development practices, then it can be helpful to describe how that tool is being used currently.So on my screen, as I see it now, I have three monitors and in the center monitor is VS code, which is maximized, but not full screen so I can still see the Windows taskbar. And in VS code, there is the title bar with the name of the workspace andThe title also has a little dot at the front of it because I'm editing a text file that has unsaved changes andthis text file is probably being written to a temporary location because if I were to close VS code without hitting control S, it would still reopen with this file with unsafe changes. So there's this like,There's a copy being saved somewhere else and the title of thethe window is the name of the workspace and then dash and then the beginning of this file name. So the file name for this untitled fileis the first few words of it before it gets truncated. And thenthe words Visual Studio Code, because we want to have the actual name of the app be in the title. So inside the body of the application, there's a left panel, a bottom panel, a right panel, and then kind of the main content area. And inside the main content area, there are four tabs open. So each of the tabs has a different text document that is open. And those text documents have file extensions determined by  the part after the rightmost period in the file name, and there's an icon to the left of thatLike each tab shows the file name and to the left of the file name is the icon and that icon is determined from the file extension.So we can imagine that we're going to want a mechanism that maps from a string to an icon. And that mapping is like icon for file extension.and...a file that has aunknown extension, like there's no period in it, then there exists the Google Magica tool, MAGIKA, wherewe can get.educated guess so it might be a PDF file that's had the .pdf removed kind of thing. So that is also like a new tool that we'd want to incorporate in our toolbox.So we have a mapping from a file path to aicon andthat mapping can try and read the file extension and can also confer with Magica. So if it's a .md file but it is a PDF according to Magica, then we'd probably want to raise some visual indication to the user that this file is not what it appears.so we could do that bybetween the two icons.creating a hybrid.So as we are messing with file explorer stuff.We currently have this pick interface with like these icons and labels underneath each icon. And there's also some hover behavior, which is very nice.so tocreate a file explorer, you kind of have the root, and then you do a little tree view. So in VS code, for example, on the right nav, there's a panel


2026-04-25 14:55:04.762 | INFO     | __main__:start_microphone_worker:51 - Got audio, was listening
2026-04-25 14:55:06.502 | INFO     | __main__:start_transcription_worker:87 - Got result {'segments': [{'text': ' so to', 'start': 0.031, 'end': 2.309}], 'language': 'en'}
2026-04-25 14:55:06.503 | INFO     | __main__:main:360 - Transcribing...
2026-04-25 14:55:06.503 | INFO     | __main__:main:362 - Typing...
2026-04-25 14:55:14.942 | INFO     | __main__:start_microphone_worker:51 - Got audio, was listening
2026-04-25 14:55:17.746 | INFO     | __main__:start_transcription_worker:87 - Got result {'segments': [{'text': " create a file explorer, you kind of have the root, and then you do a little tree view. So in VS code, for example, on the right nav, there's a panel", 'start': 0.031, 'end': 9.295}], 'language': 'en'}
2026-04-25 14:55:17.746 | INFO     | __main__:main:360 - Transcribing...
2026-04-25 14:55:17.746 | INFO     | __main__:main:362 - Typing...
2026-04-25 14:55:19.802 | INFO     | __main__:start_microphone_worker:51 - Got audio, was listening
2026-04-25 14:55:21.980 | INFO     | __main__:start_transcription_worker:87 - Got result {'segments': [{'text': " that says Explorer and then there's a little three dot menu for some additional settings.", 'start': 0.031, 'end': 4.233}], 'language': 'en'}
2026-04-25 14:55:21.980 | INFO     | __main__:main:360 - Transcribing...
2026-04-25 14:55:21.980 | INFO     | __main__:main:362 - Typing...
2026-04-25 14:55:22.812 | INFO     | __main__:start_microphone_worker:51 - Got audio, was listening
2026-04-25 14:55:24.730 | INFO     | __main__:start_transcription_worker:87 - Got result {'segments': [{'text': ' And in that three dot menu, there is...', 'start': 0.031, 'end': 2.343}], 'language': 'en'}
2026-04-25 14:55:24.731 | INFO     | __main__:main:360 - Transcribing...
2026-04-25 14:55:24.731 | INFO     | __main__:main:362 - Typing...
2026-04-25 14:55:38.553 | INFO     | __main__:start_microphone_worker:51 - Got audio, was listening
2026-04-25 14:55:41.896 | INFO     | __main__:start_transcription_worker:87 - Got result {'segments': [{'text': " a list of little checkbox items. So there's open editors, folders, outline, timeline, NPM scripts. And I currently have open editors, folders and timeline checked where folders as grayed out as it cannot be unchecked.", 'start': 0.031, 'end': 14.898}], 'language': 'en'}
2026-04-25 14:55:41.897 | INFO     | __main__:main:360 - Transcribing...
2026-04-25 14:55:41.897 | INFO     | __main__:main:362 - Typing...
2026-04-25 14:56:06.653 | INFO     | __main__:start_microphone_worker:51 - Got audio, was listening
2026-04-25 14:56:10.973 | INFO     | __main__:start_transcription_worker:87 - Got result {'segments': [{'text': " so those checked items create little accordions so inside the explorer there's the accordion for the open editors and it says one unsaved and then it has a row for each of the editors where it has the icon the name the name and stuff is green if it's a git file that has like changes that aren't committed and again it has the dot for an unsaved file", 'start': 0.031, 'end': 25.343}], 'language': 'en'}
2026-04-25 14:56:10.973 | INFO     | __main__:main:360 - Transcribing...
2026-04-25 14:56:10.973 | INFO     | __main__:main:362 - Typing...
2026-04-25 14:56:13.693 | INFO     | __main__:start_microphone_worker:51 - Got audio, was listening
2026-04-25 14:56:15.958 | INFO     | __main__:start_transcription_worker:87 - Got result {'segments': [{'text': " And then there's the accordion for the actual folders, which in this case is the workspace. So", 'start': 0.031, 'end': 6.359}], 'language': 'en'}
2026-04-25 14:56:15.958 | INFO     | __main__:main:360 - Transcribing...
2026-04-25 14:56:15.958 | INFO     | __main__:main:362 - Typing...
2026-04-25 14:56:35.002 | INFO     | __main__:start_microphone_worker:51 - Got audio, was listening
2026-04-25 14:56:38.646 | INFO     | __main__:start_transcription_worker:87 - Got result {'segments': [{'text': " This workspace root has multiple arbitrary file locations within it. So if you imagine a file explorer starting on the C drive, then everything's like actually parented underneath that. But in this workspace view, you can have multiple disjoint parents. So there's a multiple root tree or a DAG.", 'start': 0.031, 'end': 20.956}], 'language': 'en'}
2026-04-25 14:56:38.646 | INFO     | __main__:main:360 - Transcribing...
2026-04-25 14:56:38.647 | INFO     | __main__:main:362 - Typing...
2026-04-25 14:56:40.253 | INFO     | __main__:start_microphone_worker:51 - Got audio, was listening
2026-04-25 14:56:42.267 | INFO     | __main__:start_transcription_worker:87 - Got result {'segments': [{'text': " with multiple roots if there's a short term for that.", 'start': 0.031, 'end': 4.891}], 'language': 'en'}
2026-04-25 14:56:42.267 | INFO     | __main__:main:360 - Transcribing...
2026-04-25 14:56:42.267 | INFO     | __main__:main:362 - Typing...


So during this narration, I had unfocused the text edit area. So as it was transcribing what I was saying, it was typing it while I was focused on the workspace, like the the explorer side of the VS code instead of the text edit area. So sending these keyboard inputs into a potentially dangerous area instead of just typing them like I wanted.One of the things I've observed is that there is the input method editor or whatever, the IME support, where things like emoji pickers or voice transcriptions can haveOS sponsored manipulation of a text input area that could also be something that we'd want to leverage So that while the microphone is armedin our app, it would be recording the waveform andThe transcription of that would go into a text area and then the.piping of that transcription into some other app would also be some mechanism that can be turned off while stillThe transcription is armed so that we're not piping inputs into some random app while we don't want to be.There's also some font rendering shenanigans that are the way we're rendering it.in our tests, it's likeOur font rendering needs some work done, and it's all done.not something written by hand by me so it's hard to modify but that also needs attention.And on the topic of like voice recording, so narration on activities on the computer, therecording of something like the mouse inputs or the keyboard inputs in addition to voice inputs and the screen content is something that would be important for creating a good training set but is also something that is important with data sensitivity where we want to include proper censorship and snoozing mechanisms so that when a user is performing a intentionalworkshop where they are demonstrating how to perform a task and they are recording everything, that something like a unanticipated Windows notification doesn't leak personally identifiable information.and the ability tocrop and create rules for what content should be displayed is also something that's important. So once we haveLike, we have the cursor info thing, which is observing the mouse position, but it's not like recording each of the samples it's creating.so these buffers and stuff likethe programmatic interaction of the app with an agent.could be done through the command line. So in our terminal we have theway to enumerate the open terminals by running the studio command so we can say terminal list and it will list the active terminals and we could do something similar wherewe could list recording sessions for things like the mouse location or audio information.And we'd also want to be able to synthesize these into a single timeline where theAs we are recording these events, we have the corresponding date time of each event, where if we are observing like an instant, and then another instant and doing the time elapsed, we need somewall clock observation to anchor those so that we can map them relative to each other.Same thing with the terminal. We might want to observe the content of the terminal.and observe the key inputs that have been sent to the terminal, or maybe the raw codes the terminal has responded with.and all the resize events as well.So we can imagine that we have integration with Tracy right now, or tracing. And from that, our trace log level can be our most verbose thing where we can emit our events andgather the information from that if we choose to launch in a way that is with recording enabled.it supports structured logging kind of one level deep so if we wanted to do very nested objects then we kind of have to serialize them into a string that we attach to these log events but we can like provide numbers and stuff to indicate like what key was pressed and stuff like that so instead of building asuper sophisticated recording mechanism specific to these different things. If we have our logging solution be the recording mechanism, and then we have a sophisticated way to analyze that, like the Tracy profiler,It would be a neat exercise to kind of rewrite that if that's possible, where the capture programcould is currently emitting these recording files. So we could read the Tracy source code and find out how to redo that stuff.So there's all thesethese ideas and in terms of concrete next steps theThere's two possiblegoals that are kind of the same. So if we imagine that this is a app for creating recordings of human-computer interactionsthen the act of recording the operation of a text editor or whatever, if we control that text editor, we get a much more rich informationcompared to the effort that we would have to put into hijacking VS code to find out the same kind of information. And we also get to control things more deeply.So as part of that, the recording is log statements. So we don't have to actually focus too hard on that because we can just run the app with the most verbose log level and put log statements everywhere and thenour goal transforms into rebuild vs code. So we have a terminal and our philosophy for recreating this code is that our windows will like instead of creating this panel situation whereThere's editors and tabs and panels and stuff. We can lean on the operating system and manipulate the operating system to re-implement something like tabs wherewe can imagine that each tab is a window and the main body area is a window. And the tabs, the tab windows in the body window communicate to say what the body should be displaying. And same thing for the Explorer where that could be its own window where if you want the Explorer to be on the left, then it's just a window that you can reposition by hand.One interesting thing is that the act of repositioning these windows like right now We have it showing our little decorations with the minimize maximize close button, but for like a tab You would want to close on middle click and not show all those little decorations except for like maybe the close button in VS code when I hover over a tab the X shows up on the right andBut it doesn't make sense to, like, maximize a tab.unless you wanted to have a full screen dedicated to just displaying the icon and name.So if we think about creating our own explorer thenwe want.a hierarchy.where items are collapsed or expanded and the content when they are expanded is lazily loaded.And we could build this as a Ratatouille app. And that's the things that like, how do we.Do this where?our details button.There's graphics protocols, so if we want to show an icon or a bitmap or have a shader render in some area, then I'm not familiar with the kitty image protocol.and whatever.Like we'd probably want to write it from scratch and.theSo if I look at this main menu and if I think that we could build this entire thing as aratatouille app that covers the entire window and then renders the text and stuff like doingdoing text that is different size is...like possible through tricks but not like if if we think we have so much control what's something that we could do that you don't get if you're just rendering a 2e and that would beEach element of the scene isits own kind of class where the text label below the icons could each of those be its own terminal for lack of a clear word, like a grid of cells that we're able to render text into or whatever. So kind of locking into the monospace font thing there.same thing for an explorer view. Like if we have a tree nav where items can be expanded or collapsed or whatever, then mapping that into a TUI is very clean, where that would be just kind of one big cell grid is holding all these items. But then when we have things like images involved, if we havethe client area in pixels, and we are slicing that into islands of content then.Leo algorithm for those content islands and stuff issomething that we would want to have.keyboard navigation 4.So...a command palette.In the your-muted-btw project, I also did some exploration on getting a icon for the listed microphones, which we can reuse.So if we think about that task, we have the list of microphones and we want to know what icon is appropriate for each of them, then we have the list of icons andpredicated on one specific microphone.We want to pick from that list what icon is appropriate.So that choice is something that in theory has a deterministic answer. Like if we want to mimic what the OS does, there is some general rule that determines that.But at the same time, users can also customize that icon, which we could query from the OS to update that rule. But we would also want our app to have that rule be updatable.I like the direction with the cursor info app, where it's a two way (Translator note: TUI) that we can render, and then we have the option to make it a little more rich.
So if we think aboutwhat it takes to present a list of microphones to the user for a choice, we have to grab the list of microphones, get their display name, and get their icon.So each of those is like a sub-task.So grabbing the microphones and then for each microphone, grab the display name, grab the icon, and each of those operations could haveamendment to the behavior where the user might want to change the display name or might want to update the icon.And...info from the OS, then the way that the user mutates it could be that we mutate it in the OS so that when we observe it again it has the changed value. I think that's an appropriate way to do it.which means that when we are presenting thesechoices or even in the recording window when we have the icon for the microphone, clicking that icon wouldAsk the user, do you want to change this?and presumably by clicking the icon that is the indication that yes they want to change it so we are really presenting them with the choice of what do you want to change it to while making it clear that they can back out of the operation.So if we are building some kind of TUI hybrid where...Technically, all the operations of our app can run inside a 2e, so like headless, no DirectX.And then we want to also add the DirectX layer where we can do much richer presentation than the way that we encode additional information using custom codes and stuff isI guess that's the next piece.So right now on our main menu we have icons and text and it's in a rich presentation andIn Atui app, the less rich presentation islike a pick operation where the pick operation is the name of each thing and then the icon to go with it and right nowLike that icon could turn into an emoji. So maybe it's a mapping from emoji to big icon or something.