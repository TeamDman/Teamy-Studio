Check one, two.We are going to be creating the TeamE Studio application. We're going to initialize this fresh repo from the TeamE Rust CLI repo andWe're going to update the behavior so that the command teamy-studio.exe when ran with no parameters will launch the application. And we're going to keep the home and cache stuff for later.it's going to use our DirectX learningstuff toact as a baseline for how to initialize the windows and stuff, minimizing the latency between the OS and the app.And...launched, the app should.open a window in the center of the monitor where the cursor is currently located.The window size should be 50% the width and height of the monitor that it appears on and it should be located in the center of that monitor such that the center of the window is in the center of the monitor.the window should be.transparent red 50 opacity and it should haveYou can click and drag on the red to reposition the window.So from the team you receive a lie, we're going to get the scaffolding for the command line stuff. And then we're going to create another top level module outside the CLI directory for the application like Windows stuff so that the command line where the invoke logic for the command line stuff is going to be very  lean, and it's going to if no other arguments are present, like if the optional command is not present, it's just going to call out to this other module to initialize the window and stuff.