Check one, two. so we currently have our terminal window and we've got these different panels with the stuff for like showing the output and things and i think we want a more modular approach where the actions that can be applied to the terminal are effectively abstracted to different commands so that when we want to do things like testing the scrolling behavior and stuff we would be able to use composition of these features rather than having  The puppeteering manipulation, the programmatic manipulation of the term will be something that's kind of limited to our test suite. So similar to the self-test commands, those are effectively predeterminedscripts that we are piloting the terminal with and if we want to do something like having a screenshot that we can write to a file thenIf we imagine the command line interface, the command line interface is effectively the way to serialize all the transactions that are going to happen with the terminal. And if we want to be compositional in our approach for this, then separating out theoutputarea and like the decorations on this window and controlling the visibility of them. When we're focusing on this kind of issue where there's a scrolling thing that's not working properly, we want to be able to spawn a terminal that doesn't have all these additional decorations that we can control so that it'sjust the um like we'd spawn a terminal and we wouldspawn it with the PowerShell no-profile command and we would assert that the content has a certain shape like the output or like the cells in the text of the terminal have certain values and then we could perform a zoom out and zoom in and like assert that the values are the same and we could also do snapshot testing where we start the terminal and snapshot itand then do the zoom out zoom in and the snapshot again and assert that there is a pixel perfect match becausezoom out zoom in would result in the same thing whereas if part of the prompt is being wiped out because of the zoom operation thenwould be something that we could capture and do the diff on these screenshots with.so right now we have some features that we haven't fully implemented like we have the red panel with the play and the stop button and we've got theidea of some of these things.kind of revamping the command line interface for teamy-studio.exe than we would have for example commands liketerminal create and then maybe we'd give it a name so it'd be kind of like docker where everything has the generated id and then they'd have the optional name and if you try and specify a name that is already in use then it would cause it tobail out and give you an error so that effectively makes the program kind of like a demon sowe would want to be able to proofread or like the communication between when we run the command line to open a new terminal, thenWhen we want to manipulate that terminal later, like if we have theterminalcreate command then that would give us some identifier and if we want to say terminal send keys and then pass that identifier that send keys operation is going to be communicating with the original one so if we have ourapps cache directory or whatever then I don't know if name pipes are the most appropriate way to do this message passing but that could be an easy way to get started where it wouldus specifyingthe Communication would use this I guess auto generated ID so that when we're running these terminals we can Enumerate them and list them and send snapshot commands. We'd have a terminal snapshot Save and terminal snapshot list or whatever where we couldCreateup shots or like programmatic interaction with these things and we'd also maybe want to change the design because right now we have this kind of purple area on the left which is our repositioning like our grab area for the window and if we think about the the normal terminal situation then you have like a title bar so maybethe design of that would bea little different or something likeIf we imagine this as kind of a Jupiter thing, then...Currently in our window, we have this kind of output area where we're storing diagnostic information instead of being the output of the commands in the terminal.So...if we were able to have thedisplay window or like this display panel be its own independent window that we programmatically position next to the terminal window then when we don't want it we would just not create that window and we could have commands inside the terminal that would be able to communicate with the output window and say like display an image or set the text to a new value or somethingSo trying to fix this scroll bug, I think we want to focus on the small pieces, but we also want to capture this idea on how we might take the next step in terms oftransforming teamy-studio into a cool app.


Another thought is that as we are creatingsurfaces that are very paired to how the Windows operating system works. If we want to programmatically send keys to our terminal application then it's also a like auto hotkey kind of a class of apps where being able to do that for things outside of our terminal application would also be a desirable scripting behavior.  where if we are able to record the actions that are happening and then replay them later, then that could be an easy way to create like a test scenario. So if we had a  way to launch these terminals and be emitting these events. And we already do have that with our global flag for the log file where if we run this with the log filter set to trace like how our run profiler command does, then we get all of these events which we can write to a new line did limited JSON file.that could be used to extract the keys and stuff that we want to be sent. So if we had aterminal window andWe want to...automate something like if we have aprocedure where we want to send a key sequence to an application, then the recording of that sequence is one component of that. And then the serialization of that recording is another part. And then playing that serialization is kind of the third part. So if we have aevent stream from our logging framework, then we probablyWe don't want to mix that with our curated serialization format like we could, but we want tobe.smart and dedicated about these features so if we imagine that we need kind of a canvas to record something on then we could have it spawn a like just a blank window and then we would have another window that's going to display like the history of the events and theprocess of like I want to send down arrow enter and then type some text and then hit enter again and then repeat that thecapturing of that key sequence, you would enter into this blank window and you would observe that the events are captured into this other kind of logging window.which now that I think about it issomething that we already do with the Ratatouillediagnostic app we made which is already part of our kind of testing a little bit where we've integrated slightly with it for some of our our testing but if we were to make that afeature of thestudio app that we are building, thencapturing of sequences of events and stuff I don't know that the terminal likeYeah, I think terminals can support mouse inputs where you have to enter kind of the dedicated mode or whatever, or at least send some kind of thing.So thedoing of that and the capturing I thinkPart of the most important thing for us testing right now is the ability totake the visible content of the stuff that's actually being displayed because we know that there's the scroll back buffer and then there's the stuff that's displayed on screen and if there's an issue with the stuff being viewed on the screen by the user then that could be a bug between the scroll back buffer and how it gets to the display representation on the screen. So the snapshotting is like the highest guarantee where if we do a screenshot and are doing pixel comparisons before and after  then that gives us a very high guarantee and then doing things headlessly where we're manipulating these buffers and running these things in the background is a lower disruption way to test things but might miss some of the other logic as we've experienced so trying to focus in on what the most important thing for us to be doing isTricky.And if we think about how the discrete action space for the terminal, like if we have a scroll up and scroll down and those get mapped to zooming in and zooming out on specific areas, thenwe can imagine having a command palette where we would want to be able to enumerate all the possible things that can happen and that would besomething that is...kind of the interface where theif we imagine just one big enum with all the possible actions then the parameterization of those things could be like if you're sending a key then the key could be one enum with the parameter for like is it the A key or the E key or whatever or if you go crazy then you'd have one big enum of actions where each variant is like send the E key, send the A key but that explodes in complexity very quickly so the command palette idea is the  effectively reusing what we have for the command line interface where if we have acommand line interface where you can send the zoom in action and the zoom out action, where that is parameterized by the ID of the terminal, then if we have a window ID like the communication that we're choosing to do between the running process and maybe some other process that's using this command line to interact with it, you can imagine that it's also could be parameterized based on the window ID or the process ID.where the terminals are uniquely addressable and maybe we have two windows that are displaying the same terminal, but they're at different zoom levels. So in this case, the zoom in action would be parameterized by the ID of the window that we want to zoom in and out on.so when you open the command palette and you're focused on one window then it would be able to know that like we have all these actions that are parameterized based on the window ID and it would auto populate that so that as you're using this command palette it's writing this command for you and it's actually going to run this in a terminal which increases the complexity because if we imagine that we have thetheway like we're gonna have optimization stuff so that when we're calling the command to zoom in on the current window or whatever that maybe we don't actually spawn a whole terminal or whatever to do the command line parsing but you can imagine that we'd want it to behave um indistinguishable from as if we did do thatSo if we have a terminal that is blocked and running a  process or like a program some full screen TUI then we'd want to be able to run the zoom in command on that which means the command palette where we're running this command would have to spawn its own terminal to run the command that we'd want to be able to inspect and instead of short circuiting that I think in the beginning it would be good to have that visibly open the new terminal and then run that command and then  Similar to the windows terminal where we have the exit behavior we can configure it to sayUm, like press any key to close this terminal or like press control D to restart. So we have the windows terminal stuff as kind of reference on what the close behaviors we'd want to be able to enforce. And maybe in a debug mode where we're trying to diagnose problems, we could run it in a mode where  the command palette terminal that automatically opens and executes these commands would stick around instead of maybe being in the background or short circuited so we'd have this raw path where to diagnose things or like to run in the most raw behavior is thekeyboard action for increasing the zoom level in the terminal when the control scroll up is detected similar to how the separation between the console and the PTY is like if you send control C then the window receives that event and it translates it to a SIGINT or a SIGTERM or whatever before it actually gets sent to the underlying application  And we would have something similar here where when our window app detects that we're trying to zoom in, it translates that into this invocation where it becomes the liketerminal window increases zoom level.  with the dash dash window ID of the correct window ID and that itself becomes a like terminal run and then that expression where it's this one studio binary that's invoking itself in these different ways so that we force all our internal behaviors into a public interface that can actually be programmatically invoked by other things  and we can worry about short-circuiting and stuff to increase the efficiency later where we are able to get thetime down so that when we're spawning these new terminals and stuff, if we can make that very quick, then we can put off the short-circuiting operations for later. But effectively,We want the command line to be this public interface for manipulating these things and if things are based on like each terminal gets its own window and two windows can share a terminal then the interface for increasing the zoom level isbased on the window ID. And same thing for sending keys, where if you send keys to one window that is pointing to the same terminal that another window is pointing at, then the other window will still display those keys being sent. So I think that's probably the rightlevel of abstraction wear.emit to the guarantee that we are not going to have more than one terminal per window than the window ID.can become that unique identifier as long as that window ID is unique like if we have one process which has a process ID running two terminalsand those get their own window ID.those two terminal windows can point to the same terminal, so it seems like we need to introduce our ownterminal ID which maybe cleanly maps to a process ID because when we spawn the terminal I think we're using open console or whatever like the con PTY so maybe that has its own ID from the operating system that we can also use where we want to be able toSeparate out the actions frombinding a window to a terminal wherewhenWe...Create a new...window thenIt is perhaps unbound, and you can choose to bind to an existing terminal, or to create a new terminal.all of this would be exposed to the command line so that when we run the window create command,it would have an option to specify the terminal ID, or if we don't, then it would start in a default behavior where it has a interface that lists all the existing terminals that you can pick from the arrow keys and we'd want to prioritize keyboard navigation, of course.